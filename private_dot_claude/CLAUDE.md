# Global Claude Code Instructions

@~/.codex/AGENTS.md

## Claude Code

- Treat the imported Codex `AGENTS.md` as the shared personal baseline.
- Prefer the nearest project `CLAUDE.md`, `CLAUDE.local.md`, `.claude/rules/`, or repo-standard agent instructions before broader personal defaults.
- Keep Claude Code-specific personal rules here only when they cannot be expressed in the opinionated personal baseline.
- Do not duplicate imported baseline content here; update the Codex baseline first, then add Claude-only exceptions below when needed.
