# $XDG_CONFIG_HOME/oh-my-devenv/bashrc.bash
#
# Bash-only local customizations for interactive Bash sessions. The baseline
# sources this file as a late interactive hook, after its own aliases, prompt,
# functions, and key bindings.
#
# Typical contents:
#   - Aliases for internal CLIs.
#   - Functions that wrap local workflows without encoding secrets.
#   - Prompt tweaks that only make sense in selected directories.
#
# Keep this file free of secrets. Secrets belong in secrets.sh. Non-secret
# environment variables that Bash, Zsh, and bootstrap should all see belong in
# env.sh, not here.

# --------------------------------------------------------------------------
# Example: short aliases for common local commands.
# --------------------------------------------------------------------------
# alias <short-alias>='<long-local-command> --with common flags'

# --------------------------------------------------------------------------
# Example: helper function.
# --------------------------------------------------------------------------
# local_ticket() {
#   local ticket="${1:?usage: local_ticket <TICKET-ID>}"
#   open "https://<ticket-system>/browse/${ticket}"
# }

# --------------------------------------------------------------------------
# Example: prompt prefix for selected checkouts only.
# --------------------------------------------------------------------------
# __local_prompt() {
#   case "$PWD" in
#     "$HOME"/workspace/work/*) printf '[work] ' ;;
#   esac
# }
# PS1='$(__local_prompt)'"$PS1"
